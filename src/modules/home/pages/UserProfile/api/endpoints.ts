export const USER_DETAILS = (userId: string) => `user/${userId}`;
export const FOLLOW_USER = (userId: string) => `user/follow/${userId}`;
export const FOLLOWERS = (userId: string) => `user/${userId}/followers`;

export const FOLLOWING = (userId: string) => `user/${userId}/following`;
